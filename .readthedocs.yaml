version: 2

# Build PDF & ePub
formats:
  - epub
  - pdf

python:
  install:
    - requirements: requirements.txt
  system_packages: false

build:
  os: ubuntu-20.04
  tools:
    python: "3"
  jobs:
    post_build:
      - echo "test"
